{
  "name": "rolefit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:tsc": "tsc && vite build",
    "preview": "vite preview",
    "build:all": "npm install --include=dev && npm run build && cd server && npm install --include=dev && npm run build",
    "start": "node server/dist/server.js"
  },
  "dependencies": {
    "typescript": "^5.8.2",
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^8.2.2"
  }
}
